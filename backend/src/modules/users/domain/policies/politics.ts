import { EmailPolicy } from "./email.policy";
import { UsernamePolicy } from "./username.policy";
import { PasswordPolicy } from "./password.policy";

export const Politics = {
    email: EmailPolicy,
    username: UsernamePolicy,
    password: PasswordPolicy,
}